Simple game.

For *nix use 
for i in *.cpp ; do g++ -std=c++11 -I /usr/include/SDL -c $i ; done
g++ -lSDL -lGL -lGLU -lSDL_ttf -lSDL_image *.o -o Streams

(thx Deirz)

For Win visual studio project included.